<%= form_for(@vlan) do |f| %>
  <% if @vlan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vlan.errors.count, "error") %> prohibited this vlan from being saved:</h2>

      <ul>
      <% @vlan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :gateway %><br>
    <%= f.text_field :gateway %>
  </div>
  <div class="field">
    <%= f.label :netMask %><br>
    <%= f.text_field :netMask %>
  </div>
  <div class="field">
    <%= f.label :tag %><br>
    <%= f.number_field :tag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
