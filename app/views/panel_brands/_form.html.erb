<%= form_for(@panel_brand) do |f| %>
  <% if @panel_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@panel_brand.errors.count, "error") %> prohibited this panel_brand from being saved:</h2>

      <ul>
      <% @panel_brand.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
