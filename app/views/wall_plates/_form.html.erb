<%= form_for(@wall_plate) do |f| %>
  <% if @wall_plate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wall_plate.errors.count, "error") %> prohibited this wall_plate from being saved:</h2>

      <ul>
      <% @wall_plate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
